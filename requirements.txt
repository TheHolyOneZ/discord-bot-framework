# The command below is for Linux servers (e.g., Pterodactyl). Comment it out if you want to download Torch. w
# --extra-index-url https://download.pytorch.org/whl/cpu



# If you're experiencing storage (disk space) issues, comment out the following modules: Torch, TorchVision, opencv.

discord.py==2.6.3
aiofiles==24.1.0
PyNaCl==1.5.0
spotipy==2.25.1
aiohttp==3.12.14
yt-dlp==2025.6.30
Pillow==10.1.0
opencv-python==4.8.1.78
numpy==2.2.6
torch==2.8.0+cpu
torchvision==0.23.0
pytz==2025.2
python-dateutil==2.9.0.post0
jsonschema==4.24.0
matplotlib==3.10.3
chat_exporter==2.8.4
PyYAML==6.0.2
psutil==5.9.6
aiosqlite==0.21.0
cryptography==45.0.7
python-dotenv==1.0.0
google-generativeai==0.8.5
rarfile==4.2
rich==14.0.0

# Many of these are NOT used by the default framework. These are for the extensions/cogs from https://zygnalbot.com/extension/. 
# So if you install all that directly, you can, 99% of the time, just install, load them, and it works.
# If you install all of them now, you won't have problems unless you break something or I forgot a requirement. 
# If this happens, go on, join the Discord, and I'll help you properly.

